{
  "name": "cider-ci_reverse-proxy",
  "version": "1.0.0",
  "description": "Reverse proxy for developing and testing Cider-CI. ",
  "main": "lib/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build": "npm install && node_modules/.bin/coffee -o lib/ -c src/"
  },
  "author": "",
  "license": "APGL",
  "dependencies": {
    "http-proxy": "^1.8.1",
    "js-yaml": "^3.2.7",
    "node.extend": "^1.1.3"
  },
  "devDependencies": {
    "coffee-script": "^1.9.1",
    "coffeeify": "^1.0.0",
    "watchify": "^2.1.1"
  }
}
